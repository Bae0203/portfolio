import styled from "styled-components";

export const Title = styled.p`
  font-size: 2.4rem;
  letter-spacing: 0.2rem;
  color: #4a4d57;
  margin-bottom: 1rem;
  word-break: keep-all;
`;

export const Context = styled.p`
  font-size: 1.6rem;
  letter-spacing: 0.2rem;
  color: #777;
  width: 41rem;
  word-break: keep-all;
`;

export const ContentWarp = styled.nav`
  margin: 10rem;
`;
