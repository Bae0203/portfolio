import styled from "styled-components";

export const SloganBackground = styled.div`
  z-index: -1;
  position: relative;
  width: 100%;
  text-align: center;
`;

export const Title = styled.p`
  padding-top: 20rem;
  font-size: 3.375rem;
  letter-spacing: 0.1em;
  font-weight: 500;
`;

export const subTitle = styled.p`
  margin-top: 5rem;
  font-size: 1.2rem;
  letter-spacing: 0.4em;
  line-height: 2rem;
`;

export const WrapSubTitle = styled.nav`
  margin-bottom: 20rem;
`;
